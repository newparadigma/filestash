# Dockerized filestash
# Instruction
1. Create docker-compose.yml file from blank.
```bash
cp docker-compose.example.yml docker-compose.yml
```
2. Run docker container
```bash
docker-compose up -d
```